# Load tests (k6)

- **E11** — sustained redemption load, "zero failed transactions".
- **R-01-07** — end-to-end capacity: measured through the edge, the pooler, the
  audit trigger and the Realtime fan-out — **not** as a microbenchmark of
  `redeem_ticket()` alone.

Scripts land in F1-03 / F2-06. Skeleton only.
