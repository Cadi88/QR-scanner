-- Seed data returns with the real schema in F1-01 (event + tickets with fixed
-- test tokens, generated by the same issuer as CSV import — see plan §6).
-- Empty for now: the only migration is roles, which needs no seed.
